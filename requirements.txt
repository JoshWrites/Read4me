# Core
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0

# Audio I/O
soundfile>=0.12.1       # WAV file output

# Model hub & format
huggingface_hub>=0.20.0
safetensors>=0.4.0
transformers>=4.40.0

# Audio processing (librosa optional — falls back to torchaudio)
librosa>=0.10.0

# Chatterbox Turbo (pip package — installs alongside the vendored standalone)
chatterbox-tts>=0.1.6
pyloudnorm>=0.1.0        # required by chatterbox.tts_turbo
resemble-perth>=1.0.1    # Resemble AI's perth fork — provides PerthImplicitWatermarker

# Chatterbox model dependencies
conformer>=0.3.2
x-transformers>=2.0.0
s3tokenizer>=0.2.0
onnx>=1.19.0
protobuf>=3.20.0
omegaconf>=2.3.0
diffusers>=0.35.0
einops

# TUI
textual>=0.60.0
rich>=13.0.0
